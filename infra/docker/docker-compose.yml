services:
  web:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile.web
    image: goalyst-web:local
    ports:
      - "3000:3000"
    env_file:
      - ../../apps/web/.env.local
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
